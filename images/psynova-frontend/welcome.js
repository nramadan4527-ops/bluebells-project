function register(type) {
  alert("Register as " + type);
  // تقدر تربطها بصفحة تسجيل
  // window.location.href = type + "-register.html";
}

function login(type) {
  alert("Login as " + type);
  // window.location.href = type + "-login.html";
}